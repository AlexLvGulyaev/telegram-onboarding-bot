# 📖 Telegram Onboarding Bot · USER_GUIDE

**Проект:** telegram-onboarding-bot
**Дата:** 2026-08-11
**Статус:** Руководства для конечного пользователя и оператора/администратора.

> 📌 **SOT:** Сценарии описаны по фактической реализации бота
> (`bot/handlers/onboarding.py`, `bot/handlers/admin.py`). Если поведение бота
> расходится с этим руководством — исправляется руководство.

---

## 🎯 1. Назначение

Бот проводит сотрудника по обучающему сценарию: объясняет материал, затем
тестирует и сохраняет результат. У бота две роли пользователей:

| Роль | Что делает |
|------|------------|
| **Сотрудник** | Проходит обучение и тест по заданной теме |
| **Оператор / администратор** | Управляет темами обучения и просматривает результаты |

---

## 🧑‍🎓 2. Руководство сотрудника

### 2.1. Как пройти обучение

1. Откройте бота в Telegram и отправьте `/start`.
   Бот поздоровается, назовёт тему и количество вопросов теста.
2. Отправьте своё имя (минимум 2 символа).
3. Бот пришлёт обучающий блок по теме. В конце он спросит, готовы ли вы к тесту.
4. Когда готовы — отправьте `Готов` (или «готов», «давай», «тест», «переходим»).
5. Бот задаст первый вопрос. Отвечайте обычным сообщением.
6. После каждого ответа бот оценит его по материалу, даст короткую обратную
   связь и задаст следующий вопрос.
7. После последнего вопроса бот покажет итог: балл, процент, сильные стороны
   и что стоит повторить. Результат сохраняется в базе.

> 💡 **Совет:** отвечайте своими словами — бот оценивает по смыслу материала, а
> не по точному совпадению слов. На один вопрос — один ответ.

### 2.2. Сигналы готовности

Бот переходит к тесту по сигналам: «готов», «давай», «тест», «экзамен»,
«переходим». Если просто написать «Готов» в конце обучения — тест начнётся.

### 2.3. Отмена

Отправьте `/cancel`, чтобы прервать сессию. После отмены бот попросит
`/start` для новой сессии.

### 2.4. Что бот не делает

- Не подсказывает ответы на вопросы теста.
- Не позволяет вернуться к обучению после начала теста.
- Не завершает тест, пока не проверит все вопросы.

---

## 🎛️ 3. Руководство оператора / администратора

### 3.1. Доступ

Админ-команды доступны только пользователю с Telegram ID, указанным в
`ADMIN_USER_ID` в `.env`. Проверка: `message.from_user.id == ADMIN_USER_ID`.
Если переменная не задана — админ-команды отключены. Узнать свой Telegram ID
можно у бота `@userinfobot`.

### 3.2. Управление темами

| Команда | Назначение |
|---------|------------|
| `/admin` | Показать меню администратора |
| `/new_topic` | Создать тему по шагам в диалоге: id, название, описание, материал |
| `/list_topics` | Список тем, активная отмечена |
| `/set_topic <id>` | Сделать тему активной по умолчанию |
| `/delete_topic <id>` | Удалить тему |

- Созданная через `/new_topic` тема сохраняется в базу и сразу доступна для
  обучения.
- Активная тема восстанавливается из `bot_settings` после пересборки контейнера
  — повторно выбирать её не нужно.
- `/set_topic` меняет тему по умолчанию для всех новых сессий.

### 3.3. Смена темы в сессии

Команда `/topic` без аргументов показывает список тем; `/topic <id>` —
переключает тему. Следующий `/start` начнёт обучение по новой теме.

### 3.4. Просмотр результатов

Результаты хранятся в PostgreSQL. Подключение — см. `DEPLOYMENT_GUIDE.md`
(раздел «Проверка базы данных»). Записи в таблице `training_results` содержат:
имя сотрудника, тему, количество вопросов, число правильных ответов, процент,
итоговую сводку и число потраченных токенов.

### 3.5. Добавление темы вручную (без диалога)

1. Создайте `topics/<id>.json`:

```json
{
  "id": "my-topic",
  "name": "Моя тема",
  "description": "Краткое описание",
  "material": "Материал для обучения...",
  "prompts_version": "v1"
}
```

2. Установите `ACTIVE_TOPIC=my-topic` в `.env`.
3. Пересоздайте контейнер: `docker compose up -d --force-recreate bot`.

Подробнее — в `DEPLOYMENT_GUIDE.md`.

---

## 🧪 4. Проверочные сценарии

Полный набор E2E-сценариев (happy-path, отмена, смена темы, некорректный ввод,
администрирование) — в `E2E_SCENARIOS.md`. Результаты прогонов и статус
дефектов — в `TESTING.md`.

---

## 📚 5. Связанные документы

- [🏠 `README.md`](../README.md) — назначение проекта и быстрый старт.
- [🚀 `docs/DEPLOYMENT_GUIDE.md`](DEPLOYMENT_GUIDE.md) — запуск, env vars, подключение к БД.
- [🎬 `docs/E2E_SCENARIOS.md`](E2E_SCENARIOS.md) — сквозные сценарии.
- [🧪 `docs/TESTING.md`](TESTING.md) — результаты тестирования и дефекты.
- [🔌 `docs/API_CONTRACT.md`](API_CONTRACT.md) — контракты внешних интеграций.